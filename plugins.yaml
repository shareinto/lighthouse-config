approve:
- lgtm_acts_as_approve: false
  repos:
  - shareinto/lighthouse-config
  require_self_approval: true
config_updater:
  gzip: false
  maps:
    config.yaml:
      name: config
    plugins.yaml:
      name: plugins  
plugins:
  shareinto/lighthouse-config:
  - config-updater
  - approve
  - lgtm  
